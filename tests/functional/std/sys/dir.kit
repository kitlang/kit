import kit.sys.dir;

function main() {
    var path = "tests/functional/std/sys/";
    for dir in Directory.read(path) {
        var path = (*dir).path;
        if path == "dir.kit" {
            printf("found dir.kit\n");
        }
    }
}
