function main() {
    var b = defined printf;
    puts(if b then "true" else "false");

    static if defined printf {
        puts("printf exists");
    } else {
        puts("printf doesn't exist");
    }
    static if defined printf123987 {
        puts("printf123987 exists");
    } else {
        puts("printf123987 doesn't exist");
    }
    static if defined __STDC_VERSION__ {
        puts("__STDC_VERSION__ exists");
    } else {
        puts("__STDC_VERSION__ doesn't exist");
    }
}
