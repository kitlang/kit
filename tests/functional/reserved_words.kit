function main() {
    var @while = "while";
    var @do = "do";

    printf("%s %s\n", @while, @do);

    var @true = true;
    var @false = false;

    if @true {
        printf("true\n");
    }
    if @false {
        printf("false\n");
    }
}
