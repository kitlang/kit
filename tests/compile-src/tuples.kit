include "stdio.h";

function main() {
    var x: (Int, CString) = (1, "something");
    var y: (CString, Int) = ("something", 2);
    var z: (CString, Int) = ("something", 2);

    // if y == z {
    //     printf("equal\n");
    // }
}
