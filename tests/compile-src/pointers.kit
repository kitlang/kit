function main() {
    var a = 1;
    var b: Ptr[Int] = a;
    printf("%i\n", *b);

    // type param inference for pointers
    var c = 2.5;
    var d: Ptr = c;
    printf("%.1f\n", *d);

    var e = b[0];
    printf("%i\n", e);
}
