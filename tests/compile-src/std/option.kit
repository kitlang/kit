function main() {
    var a: Option = Some(1);
    match a {
        Some(i) => {
            printf("%i\n", i);
        }
        default => {
            printf("failure\n");
        }
    }
    // a.unwrap();

    var b: Option[Int] = None;
    match b {
        Some(i) => {
            printf("failure\n");
        }
        default => {
            printf("success\n");
        }
    }
}
