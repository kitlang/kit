import kit.sys.dir;

function main() {
    var path = "tests/compile-src/std/sys/";
    var d = readDir(path);
    for dir in d {
        var path = (*dir).path;
        if path == "dir.kit" {
            printf("found dir.kit\n");
        }
    }
}
