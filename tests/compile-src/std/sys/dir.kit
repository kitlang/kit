import kit.sys.dir;

function main() {
    var path = "tests/compile-src/std/sys/";
    for dir in readDir(path) {
        var path = (*dir).path;
        if path == "dir.kit" {
            printf("found dir.kit\n");
        }
    }
}
