#pragma once

#include "tokens.h"

/// Attempt to tokenize the given `source` as an array of `tokens`, an out parameter.
bool tokenize(String source, Token* tokens) {
  // TODO: Scan an input string into a list of tokens

  return false;
}
